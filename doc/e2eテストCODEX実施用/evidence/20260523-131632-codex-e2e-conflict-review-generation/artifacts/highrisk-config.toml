[sources]
include = ["docs/spec/**/*.md"]
exclude = ["**/drafts/**"]

[core]
purpose_file = "docs/core/purpose.md"
concept_file = "docs/core/concept.md"

[context]
storage = ".spec-anchor/context"

[section]
max_heading_level = 4

[section_metadata]
summary_enabled = true
search_keys_enabled = true
related_sections_enabled = true

[chapter_anchor]
enabled = true

# LLM provider 定義。/spec-core が保持物の生成に使う。
# /spec-inject と /spec-realign の会話解釈・制約生成は Agent 環境が担うため対象外。

[llm.providers.conflict_e2e]
command = "/tmp/20260523-131632-codex-e2e-conflict-review-generation.projects/highrisk/tools/conflict-provider.py highrisk /home/kazuki/public_html/spec-anchor/doc/e2eテストCODEX実施用/evidence/20260523-131632-codex-e2e-conflict-review-generation/artifacts/highrisk-provider-invocations.jsonl"
model = "conflict-provider-highrisk"
effort = "low"
timeout_sec = 30
max_retries = 0

[llm.providers.codex]
command = "codex"
model = "gpt-5.4-mini"
effort = "low"
timeout_sec = 120
max_retries = 1

[llm.providers.claude_typing]
command = "claude"
model = "claude-sonnet-4-6"
effort = "low"
timeout_sec = 360
max_retries = 1

[llm.providers.claude_judge]
command = "claude"
model = "claude-sonnet-4-6"
effort = "low"
timeout_sec = 360
max_retries = 1

# 各 stage がどの provider を使うかを指定する。
[llm.stage_routing]
section_metadata   = "conflict_e2e"
related_sections   = "conflict_e2e"
conflict_review    = "conflict_e2e"
chapter_key_anchor = "conflict_e2e"

[retrieval]
dense_top_k = 12
sparse_top_k = 20
rank_fusion = "rrf"
section_collection = "spec_anchor_sections_20260523_131632_conflict_highrisk"
section_dense_threshold = 0.55
section_candidate_top_k = 16
section_final_top_n = 8

[embedding]
provider = "flagembedding"
model = "BAAI/bge-m3"
dense_enabled = true
sparse_enabled = true

[vector_store]
provider = "qdrant"
url = "http://localhost:6333"

[limits]
section_summary_max_chars = 480
search_keys_max = 32
related_candidate_max_per_section = 32
related_selected_max_per_section = 8
conflict_pair_max_per_section = 8
llm_batch_max_sections = 8
llm_batch_max_chars = 12000
llm_batch_concurrency = 4   # 1 = 逐次。Pro 5x / Max 5x なら 4-8 推奨

[watcher]
enabled = true
interval_ms = 2000
debounce_ms = 1000
stale_lock_ms = 300000
state_file = ".spec-anchor/state/watch_state.json"
queue_file = ".spec-anchor/state/watch_queue.json"
