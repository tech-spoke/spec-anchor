# TODO テンプレート

このファイルは spec-anchor の **大きな課題ごとの TODO ファイル** の雛形である。新しい課題が発生したら、本ファイルを `doc/TODO/<topic>.ja.md` (例: `doc/TODO/specclaim_migration.ja.md`) としてコピーし、各セクションを埋めて運用する。完了済みの課題 TODO ファイルは `doc/TODO/完了済みTODO/<YYYY-MM-DD>_<topic>.ja.md` に move する。

本テンプレート自体はリポジトリに常駐し、課題追加時の参照基準となる。CLAUDE.md / AGENTS.md からは `doc/TODO/*.ja.md` (進行中の課題 TODO ファイル群) と `doc/TODO_template.ja.md` (本ファイル) の両方を参照する。

---

以下、`doc/TODO/<topic>.ja.md` を新規作成する時にコピーする雛形。

---

# TODO: <課題名 (1 行で簡潔に)>

**起票日**: YYYY-MM-DD
**起票者**: Claude main | CODEX | Human
**最終更新**: YYYY-MM-DD
**ステータス**: 計画中 | 実装中 | 検証中 | 完了
**関連設計書**: (該当ある場合、`doc/EXTERNAL_DESIGN.ja.md §X.Y` / `doc/DESIGN.ja.md §X.Y` / 別 design doc へのリンク)

## 全体目的

本課題を解決することで何を達成するか、1〜3 段落で記述する。利用者視点 (どんな workflow / 観測可能な状態が改善されるか) と設計判断の根拠 (なぜこの課題が優先されるか、なぜこのアプローチを取るか) を含める。完了したら何を以て成功とみなすかも、ここで宣言する。

このセクションは課題の存在意義であり、sub task のどれかが変更されても揺らがないレベルで安定させる。揺らぐようなら課題の切り方を見直す。

## 状況サマリー

| # | sub task ID | 概要 | 状態 | 残作業 | 最終更新 | 完了 commit |
|---|---|---|---|---|---|---|
| 1 | T-... | ... | 未着手 / 着手中 / コア実装完了 / regression test 待ち / 完了 | ... | YYYY-MM-DD | — |
| 2 | T-... | ... | ... | ... | YYYY-MM-DD | — |

本表の `状態` と `残作業` を見るだけで「次に何をすべきか」が分かるように維持する。

## sub task 詳細

### T-task-1: 1 行概要

**状態**: 未着手 | 着手中 | コア実装完了 | regression test 待ち | 完了
**担当**: Claude main | CODEX | Human
**最終更新**: YYYY-MM-DD
**直近 commit**: (該当ある場合)

#### 背景

何が問題で、なぜこの sub task が必要か。利用者視点で書く。

#### 真因 / 対応方針

真因が確定している場合は記述。確定していない場合は仮説と確認手順を書く。

#### 検証条件

完了確認に必要な test や実機実行条件を列挙。

#### 完了条件

何が達成されたら完了扱いにするか。

#### 残作業

- ...
- ...

#### 依存 / scope 外

依存 task / 本 sub task の scope 外として明示すべき事項。

### T-task-2: 1 行概要

(同じ構造で続ける)

## 課題全体の完了条件

すべての sub task が完了 + 統合的な検証 (実機 + recall + regression 等) が達成されたら、本課題を完了扱いにする。具体的な完了基準を列挙する:

- ...
- ...

## 依存 / scope 外

他課題との依存関係や、本課題の scope 外として明示すべき事項。

## sub task / 課題完了時の更新手順

sub task が完了したら、次の 2 箇所をセットで更新する (片方だけだと「状況サマリーは完了、本文は未完了」のような不整合が残る):

1. 「## 状況サマリー」表の該当行
   - `状態` 列を「完了」に変更
   - `残作業` 列を空欄または「—」に変更
   - `最終更新` 列に完了日 (YYYY-MM-DD) を記入
   - `完了 commit` 列に commit hash を記入

2. 「## sub task 詳細」の該当 sub task セクション
   - `**状態**` を「完了」に変更
   - `**最終更新**` を完了日に変更
   - `**直近 commit**` に完了 commit hash を記入
   - **章タイトル末尾に完了マークを付ける**。例:
     ```markdown
     ### [完了 2026-05-29, commit abc1234] T-task-1: 1 行概要
     ```
   - 「#### 残作業」セクションは空にするか、「(完了)」と書く

課題全体が完了したら、追加で次を更新する:

3. ファイル冒頭メタデータ
   - `**ステータス**` を「完了」に変更
   - `**最終更新**` を完了日に変更
   - `**直近 commit**` に代表 commit (= 課題完了を宣言する commit) を記入

4. 「## 状況サマリー」表のすべての sub task 行が「完了」になっていることを確認

5. 下記「## archive 手順」に進む

## archive 手順

1. すべての sub task と「課題全体の完了条件」が達成されていることを確認する
2. 本ファイル全体を `doc/TODO/完了済みTODO/<YYYY-MM-DD>_<topic>.ja.md` に `git mv` する
