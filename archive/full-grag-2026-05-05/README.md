# full GRAG Archive 2026-05-05

This directory preserves the pre-lightweight SPEC-grag state.

- `doc/` contains the old full GRAG design documents and phase reports.
- `root/` contains the old root-level implementation, templates, tests, configs, and local runtime directories moved out of the project root.

The current source of truth is the lightweight design in the repository root `doc/`.
