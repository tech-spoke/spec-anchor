# Purpose

spec-grag artifact-layout refactor の real-env smoke check として、最小の Source Specs と Purpose / Core Concept で `/spec-core` の `--all` / incremental / `--rebuild` 各経路を実行確認する。

このプロジェクトの恒久目的は spec-grag CLI 自身の動作検証用 fixture であり、外部利用者向けではない。
